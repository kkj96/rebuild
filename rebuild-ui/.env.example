# API Configuration
VITE_API_URL=http://localhost:8000

# Configuration Mode: embedded | local | remote
VITE_CONFIG_MODE=embedded

# Remote config URL (only used when VITE_CONFIG_MODE=remote)
# VITE_CONFIG_URL=https://api.example.com/config
